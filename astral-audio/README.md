# astral-audio

An extension to VSCode to convert WAV Files to MP3 from VSCode 

# How to use

Right click on a folder or on a `.wav` file to convert files. 
* You should see `convert WAV to MP3` in the context menu of a file
* on folders you should see `Convert WAV in directory to MP3`